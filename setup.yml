---
- hosts: all
  become: true
  gather_facts: false
  
  tasks:

    - name: install docker 
      apt:
        name: docker 
        state: present
        update_cache: true

    - name: start docker
      systemd:
        name: docker
        state: started
    
    